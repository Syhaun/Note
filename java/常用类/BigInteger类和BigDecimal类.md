#BigInteger类
BigInteger(String)

构造方法来创建实例

add,subtract,multipe,divide和remainder方法来进行算数运算

nextProbablePrime()

返回一个大于此的可能为素数的第一个整数

isPorbablePrime(int certainty)

判断是不是素数
#BigDecimal
其对象可以达到任意精度

toBigInteger()

对象转换