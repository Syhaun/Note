#数组基础
数组一旦被创建，大小就已近固定了，可以使用一个数组引用变量和下标来访问数组中的元素
###数组声明
```
元素类型[] 数组名
元素类型[][] 数组名
```
###创建数组
```
元素类型[] 数组名 = new 元素类型[数组大小];
元素类型[][] 数组名 = new 元素类型[数组大小][];
```
###获取数组的长度
```
数组名.length
二维数组
数组名[n].length  
