# 模型的建立

首先，可以使用极坐标系来描述无人机的位置。假设圆心无人机的位置为 (0,0)，圆周上无人机的位置为 (ri?,θi?)，其中 ri? 表示无人机到圆心的距离，θi? 表示无人机与圆心连线与极轴的夹角。

其次，可以定义一个目标函数来衡量无人机位置与理想位置之间的偏差。例如，可以定义目标函数为

`f=∑i=19?(ri??100)2+∑i=19?(θi??9360?(i?1))2`

其中第一项表示无人机到圆心距离与理想距离之间的偏差，第二项表示无人机与极轴夹角与理想夹角之间的偏差。

最后，可以使用优化算法来求解最优调整方案。在每次迭代中，选择编号为 FY00 的无人机和圆周上最多 3 架无人机遂行发射信号，其余无人机根据接收到的方向信息调整位置，以最小化目标函数 f。

# 模型的求解

梯度下降法：梯度下降法是一种常用的优化算法，它通过沿着目标函数梯度的负方向迭代更新参数来最小化目标函数。在这个问题中，可以使用梯度下降法来更新无人机的位置，以最小化目标函数 f。具体来说，在每次迭代中，可以计算目标函数关于无人机位置的梯度，然后沿着梯度的负方向更新无人机的位置。

为了解决这个问题，我们可以使用梯度下降法来求解最优调整方案。

1. 初始化无人机的位置为表 1 中给出的初始位置。
2. 计算目标函数` f=∑i=19?(ri??100)2+∑i=19?(θi??9360?(i?1))2 `的梯度。
3. 沿着梯度的负方向更新无人机的位置，即` ri?←ri??α?ri??f`?，`θi?←θi??α?θi??f`?，其中 α 是学习率。
4. 重复步骤 2 和步骤 3，直到满足停止条件。

在这个算法中，梯度下降法通过迭代更新无人机的位置来最小化目标函数 f。每次迭代中，算法计算目标函数的梯度，然后沿着梯度的负方向更新无人机的位置。通过多次迭代，无人机的位置会逐渐接近理想位置，最终达到均匀分布在一个圆周上的目标。.
