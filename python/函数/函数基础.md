# 函数的定义

```
def 函数名(传入参数):
    函数体
    return 返回值
```

# 函数的调用

`函数名(参数)`

![Alt text](image.png)

# 函数的参数

在计算的时候,接受外部(调用时)提供的数据

![Alt text](image-1.png)

# 函数的返回值

![Alt text](image-2.png)

  ==函数体在遇到return后就结束了,所以写在return后的代码不会执行==

  # None

  ![Alt text](image-3.png)